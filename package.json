{
  "name": "mydesktop",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "server/*"
  ],
  "scripts": {
    "build": "npm run -w @crivayne/desktop build && npm run -w @crivayne/shared-ui build",
    "dev": "npm run -w @crivayne/desktop dev"
  }
}